import React from 'react'

const Loading = () => {
    // if products are not available then this loading page will show
   return (
    <div className='w-full h-full flex justify-center items-center'>
        <h1 className='text-6xl'>Loading</h1>
    </div>
  )
}

export default Loading